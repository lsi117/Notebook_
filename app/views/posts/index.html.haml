- if user_signed_in?
  %p#intro
    Hey there!
    = current_user.name
    %br/
    %span
      Start Sharing!
      %i.fa.fa-pencil

-else
  %p#intro
    Welcome to Notebook, where programmers can share helpful notes.
    %br/
    %span
      Start Sharing!
      %i.fa.fa-pencil

#posts
  - @posts.each do |post|
    .post
      .post_image
        = link_to (image_tag post.image.url(:thumb)), post
      .post_content
        .title
          %h1= link_to post.title, post
        .data.clearfix
          %p.username
            Shared by:
            = post.user.name
          %p.buttons
            %span
              %i.fa.fa-comments-o
              = post.comments.count
            %span
              %i.fa.fa-thumbs-o-up
              = post.get_likes.size
            %span
              %i.fa.fa-thumbs-o-down
              = post.get_dislikes.size
